# rainwarrior (Brad Smith) SNES hi-res demo ROMs — FNV-1a framebuffer golden hashes.
#
# The ROMs live in the gitignored tests/roms/external/rainwarrior/ tier (no explicit upstream
# license — usable locally, NOT redistributable, so only these DERIVED hashes are committed, per
# docs/adr/0003). The gate self-skips when the ROM dir is absent, so CI (which lacks the ROMs) is
# unaffected; a local developer who downloads them from
# https://github.com/bbbradsmith/SNES_stuff gets the check.
#
# Blessed at 60 frames and cross-validated against MesenCE (scripts/perdot_capture.lua, MCE_FRAMES=60)
# via canonical distinct-color SETS (docs/adr/0013 posture): twoship matches MesenCE exactly (26/26
# colors, 0 diff — validates the Mode 5 hi-res path); elasticity shares all 1509 of its colors with
# MesenCE, which renders 2 extra ($1999/$5fbf) attributable to MesenCE's known-wrong master-brightness
# formula (N/15 vs RustySNES's hardware-correct (N+1)/16 per fullsnes; see the MesenCE brightness
# caveat in scripts/perdot_crossval.sh), NOT a RustySNES defect.
#
# name	fnv1a_hash
twoship	70686b4adf16cd63
elasticity	a85a7d5e6a73b2de

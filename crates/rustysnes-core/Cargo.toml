[package]
name = "rustysnes-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
rustysnes-cpu = { path = "../rustysnes-cpu", version = "0.1.0", default-features = false }
rustysnes-ppu = { path = "../rustysnes-ppu", version = "0.1.0", default-features = false }
rustysnes-apu = { path = "../rustysnes-apu", version = "0.1.0", default-features = false }
rustysnes-cart = { path = "../rustysnes-cart", version = "0.1.0", default-features = false }

[lints]
workspace = true

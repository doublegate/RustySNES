[package]
name = "rustysnes-ppu"
version = "1.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
bitflags = "2"
rustysnes-cart = { path = "../rustysnes-cart", version = "1.0.1", default-features = false }
rustysnes-savestate = { path = "../rustysnes-savestate", version = "1.0.1" }

[lints]
workspace = true

name: Deploy Pages (demo + docs)
on:
  push:
    branches: [main]
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - uses: actions/checkout@v5
      # Build the wasm demo (trunk) at / and the rustdoc at /api/, then deploy ONE artifact.
      # GOTCHA: keep web/Trunk.toml's wasm-bindgen pin == Cargo.lock library version.
